def join(L):
    joinStr = ""
    for s in L:
        joinStr = joinStr+s+" "
    return joinStr


def join(L, separator):
    joinStr = ""
    for s in L:
        joinStr = joinStr+s+separator
    return joinStr

